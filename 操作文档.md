# 加密货币量化交易系统 - 完整操作文档

## 📋 目录

1. [系统概述](#系统概述)
2. [系统功能](#系统功能)
3. [快速开始](#快速开始)
4. [详细功能说明](#详细功能说明)
5. [命令行使用](#命令行使用)
6. [Python API 使用](#python-api使用)
7. [配置文件说明](#配置文件说明)
8. [常见问题](#常见问题)

---

## 系统概述

**加密货币量化交易系统 v3.0** 是一个专业的量化交易回测和分析平台，支持：

- ✅ 多策略回测（EMA+RSI 策略、多因子策略）
- ✅ 盈利交易分析和策略优化
- ✅ 黑天鹅事件防护
- ✅ 因子分析和研究
- ✅ 参数优化
- ✅ 完整的风险指标统计

---

## 系统功能

### 1. 数据获取模块 (`data/fetch_data.py`)

#### 1.1 核心功能

**主要功能：**

- ✅ 支持从 OKX 等交易所获取历史 K 线数据
- ✅ 自动数据缓存（Parquet 格式，提高加载速度）
- ✅ 支持多时间框架（1m, 3m, 5m, 15m, 30m, 1h, 2h, 4h, 6h, 8h, 12h, 1d, 3d, 1w, 1M）
- ✅ 批量获取大量历史数据（自动分批，支持数万根 K 线）
- ✅ 代理支持（HTTP/HTTPS 代理）
- ✅ 按日期范围获取数据
- ✅ 多时间框架批量获取
- ✅ 获取可用交易对列表

**核心特性：**

- 🔄 智能缓存机制：避免重复请求，缓存过期时间可配置
- 🔍 自动去重和排序：确保数据完整性
- 🛡️ 异常处理和重试机制：网络错误自动重试
- 📊 进度显示：大批量数据获取时显示进度
- ⚡ 自动降级：网络失败时使用过期缓存

#### 1.2 DataFetcher 类

**初始化参数：**

| 参数                 | 类型 | 默认值         | 说明                                                                                  |
| -------------------- | ---- | -------------- | ------------------------------------------------------------------------------------- |
| `exchange_name`      | str  | `'okx'`        | 交易所名称（支持 ccxt 所有交易所）                                                    |
| `proxy`              | Dict | `None`         | 代理配置，格式：`{'http': 'http://127.0.0.1:7890', 'https': 'http://127.0.0.1:7890'}` |
| `cache_dir`          | str  | `'data/cache'` | 缓存目录路径                                                                          |
| `cache_enabled`      | bool | `True`         | 是否启用缓存                                                                          |
| `cache_expiry_hours` | int  | `1`            | 缓存过期时间（小时）                                                                  |

**支持的时间框架：**

```python
TIMEFRAME_MS = {
    '1m': 60 * 1000,        # 1分钟
    '3m': 3 * 60 * 1000,     # 3分钟
    '5m': 5 * 60 * 1000,     # 5分钟
    '15m': 15 * 60 * 1000,   # 15分钟
    '30m': 30 * 60 * 1000,   # 30分钟
    '1h': 60 * 60 * 1000,    # 1小时
    '2h': 2 * 60 * 60 * 1000, # 2小时
    '4h': 4 * 60 * 60 * 1000, # 4小时
    '6h': 6 * 60 * 60 * 1000, # 6小时
    '8h': 8 * 60 * 60 * 1000, # 8小时
    '12h': 12 * 60 * 60 * 1000, # 12小时
    '1d': 24 * 60 * 60 * 1000,  # 1天
    '3d': 3 * 24 * 60 * 60 * 1000, # 3天
    '1w': 7 * 24 * 60 * 60 * 1000, # 1周
    '1M': 30 * 24 * 60 * 60 * 1000, # 1月
}
```

#### 1.3 主要方法

##### 1.3.1 `fetch_ohlcv()` - 获取 OHLCV 数据

**功能：** 获取指定交易对和时间框架的 K 线数据

**参数：**

| 参数        | 类型 | 默认值       | 说明                                               |
| ----------- | ---- | ------------ | -------------------------------------------------- |
| `symbol`    | str  | `'BTC/USDT'` | 交易对，如 `'BTC/USDT'`, `'ETH/USDT'`              |
| `timeframe` | str  | `'1h'`       | 时间框架，如 `'30m'`, `'1h'`, `'4h'`, `'1d'`       |
| `limit`     | int  | `1000`       | 获取 K 线数量                                      |
| `since`     | int  | `None`       | 起始时间戳（毫秒），如果为 None 则从当前向过去获取 |
| `use_cache` | bool | `True`       | 是否使用缓存                                       |

**返回值：** `pd.DataFrame`，包含以下列：

- `open`: 开盘价
- `high`: 最高价
- `low`: 最低价
- `close`: 收盘价
- `volume`: 成交量
- 索引为 `Date`（DatetimeIndex）

**使用示例：**

```python
from data.fetch_data import DataFetcher

# 创建数据获取器
fetcher = DataFetcher(
    exchange_name='okx',
    cache_enabled=True,
    cache_expiry_hours=1
)

# 示例1: 获取最近1000根1小时K线
df = fetcher.fetch_ohlcv('BTC/USDT', '1h', 1000)

# 示例2: 获取最近20000根30分钟K线
df = fetcher.fetch_ohlcv('BTC/USDT', '30m', 20000)

# 示例3: 从指定时间戳开始获取
import datetime
since = int(datetime.datetime(2024, 1, 1).timestamp() * 1000)
df = fetcher.fetch_ohlcv('ETH/USDT', '4h', 5000, since=since)

# 示例4: 禁用缓存，强制从网络获取
df = fetcher.fetch_ohlcv('BTC/USDT', '1h', 1000, use_cache=False)

# 示例5: 使用代理
proxy = {
    'http': 'http://127.0.0.1:7890',
    'https': 'http://127.0.0.1:7890'
}
fetcher = DataFetcher(exchange_name='okx', proxy=proxy)
df = fetcher.fetch_ohlcv('BTC/USDT', '1h', 1000)
```

##### 1.3.2 `fetch_ohlcv_by_date()` - 按日期范围获取数据

**功能：** 按日期范围获取 OHLCV 数据（更直观的日期接口）

**参数：**

| 参数         | 类型 | 默认值       | 说明                                           |
| ------------ | ---- | ------------ | ---------------------------------------------- |
| `symbol`     | str  | `'BTC/USDT'` | 交易对                                         |
| `timeframe`  | str  | `'1h'`       | 时间框架                                       |
| `start_date` | str  | `None`       | 起始日期，格式：`'YYYY-MM-DD'`                 |
| `end_date`   | str  | `None`       | 结束日期，格式：`'YYYY-MM-DD'`，默认为当前时间 |

**返回值：** `pd.DataFrame`（格式同 `fetch_ohlcv()`）

**使用示例：**

```python
# 示例1: 获取2024年1月1日到2024年1月31日的数据
df = fetcher.fetch_ohlcv_by_date(
    symbol='BTC/USDT',
    timeframe='1h',
    start_date='2024-01-01',
    end_date='2024-01-31'
)

# 示例2: 获取从2024年1月1日到现在的所有数据
df = fetcher.fetch_ohlcv_by_date(
    symbol='ETH/USDT',
    timeframe='4h',
    start_date='2024-01-01'
)

# 示例3: 获取最近的数据（不指定日期）
df = fetcher.fetch_ohlcv_by_date(
    symbol='BTC/USDT',
    timeframe='1d'
)
```

##### 1.3.3 `fetch_multiple_timeframes()` - 获取多时间框架数据

**功能：** 一次性获取多个时间框架的数据

**参数：**

| 参数         | 类型 | 默认值               | 说明                        |
| ------------ | ---- | -------------------- | --------------------------- |
| `symbol`     | str  | `'BTC/USDT'`         | 交易对                      |
| `timeframes` | list | `['1h', '4h', '1d']` | 时间框架列表                |
| `limit`      | int  | `500`                | 每个时间框架获取的 K 线数量 |

**返回值：** `Dict[str, pd.DataFrame]`，键为时间框架，值为对应的 DataFrame

**使用示例：**

```python
# 获取多个时间框架的数据
data = fetcher.fetch_multiple_timeframes(
    symbol='BTC/USDT',
    timeframes=['30m', '1h', '4h', '1d'],
    limit=1000
)

# 访问不同时间框架的数据
df_30m = data['30m']
df_1h = data['1h']
df_4h = data['4h']
df_1d = data['1d']
```

##### 1.3.4 `get_available_symbols()` - 获取可用交易对

**功能：** 获取交易所所有可用的交易对列表

**返回值：** `list`，交易对字符串列表

**使用示例：**

```python
# 获取所有可用交易对
symbols = fetcher.get_available_symbols()
print(f"共有 {len(symbols)} 个交易对")
print("前10个交易对:", symbols[:10])

# 过滤USDT交易对
usdt_pairs = [s for s in symbols if s.endswith('/USDT')]
print(f"USDT交易对数量: {len(usdt_pairs)}")
```

#### 1.4 向后兼容接口

##### `fetch_btc_ohlcv()` - 简化接口

**功能：** 快速获取 BTC/USDT 1 小时 K 线数据（向后兼容）

**参数：**

| 参数    | 类型 | 默认值 | 说明     |
| ------- | ---- | ------ | -------- |
| `proxy` | Dict | `None` | 代理配置 |
| `limit` | int  | `500`  | K 线数量 |

**使用示例：**

```python
from data.fetch_data import fetch_btc_ohlcv

# 快速获取BTC数据
df = fetch_btc_ohlcv(limit=1000)

# 使用代理
proxy = {'http': 'http://127.0.0.1:7890', 'https': 'http://127.0.0.1:7890'}
df = fetch_btc_ohlcv(proxy=proxy, limit=2000)
```

#### 1.5 缓存机制详解

**缓存文件结构：**

```
data/cache/
├── okx_BTC_USDT_1h.parquet          # 缓存数据（Parquet格式）
├── okx_BTC_USDT_1h_meta.json        # 缓存元数据
├── okx_ETH_USDT_30m.parquet
└── okx_ETH_USDT_30m_meta.json
```

**缓存元数据格式：**

```json
{
  "symbol": "BTC/USDT",
  "timeframe": "1h",
  "last_update": "2024-01-15T10:30:00",
  "rows": 5000
}
```

**缓存策略：**

1. 检查缓存是否存在
2. 检查缓存是否过期（基于 `cache_expiry_hours`）
3. 检查缓存数据量是否足够
4. 如果缓存有效，直接返回缓存数据
5. 如果缓存无效，从网络获取并更新缓存
6. 网络失败时，尝试使用过期缓存（降级策略）

**缓存管理：**

```python
# 禁用缓存
fetcher = DataFetcher(cache_enabled=False)

# 设置缓存过期时间为24小时
fetcher = DataFetcher(cache_expiry_hours=24)

# 自定义缓存目录
fetcher = DataFetcher(cache_dir='my_custom_cache')
```

#### 1.6 错误处理

**自动重试机制：**

- 网络请求失败时自动重试
- 批次获取失败时跳过该批次继续获取
- 最终失败时尝试使用过期缓存

**常见错误处理：**

```python
try:
    df = fetcher.fetch_ohlcv('BTC/USDT', '1h', 1000)
except Exception as e:
    print(f"数据获取失败: {e}")
    # 系统会自动尝试使用过期缓存
```

#### 1.7 性能优化建议

1. **启用缓存**：对于重复获取的数据，缓存可以大幅提高速度
2. **合理设置缓存过期时间**：根据数据更新频率设置
3. **批量获取**：使用 `fetch_multiple_timeframes()` 一次性获取多个时间框架
4. **使用代理**：如果网络不稳定，配置代理可以提高成功率
5. **分批获取大量数据**：系统自动分批，无需手动处理

#### 1.8 完整使用示例

```python
from data.fetch_data import DataFetcher
from datetime import datetime

# 1. 创建数据获取器（带代理）
proxy = {
    'http': 'http://127.0.0.1:7890',
    'https': 'http://127.0.0.1:7890'
}
fetcher = DataFetcher(
    exchange_name='okx',
    proxy=proxy,
    cache_enabled=True,
    cache_expiry_hours=1
)

# 2. 获取单时间框架数据
df = fetcher.fetch_ohlcv('BTC/USDT', '30m', 20000)
print(f"数据范围: {df.index[0]} ~ {df.index[-1]}")
print(f"数据量: {len(df)} 根K线")

# 3. 按日期范围获取
df = fetcher.fetch_ohlcv_by_date(
    symbol='ETH/USDT',
    timeframe='4h',
    start_date='2024-01-01',
    end_date='2024-01-31'
)

# 4. 获取多时间框架
multi_data = fetcher.fetch_multiple_timeframes(
    symbol='BTC/USDT',
    timeframes=['1h', '4h', '1d'],
    limit=1000
)

# 5. 获取可用交易对
symbols = fetcher.get_available_symbols()
print(f"可用交易对数量: {len(symbols)}")
```

### 2. 策略模块

#### 2.1 EMA+RSI 策略 (`strategies/ema_rsi_strategy.py`)

**功能：**

- EMA 多周期趋势判断
- RSI 超买超卖信号
- ADX 趋势强度过滤
- MACD 确认信号
- ATR 动态止损止盈
- 移动止损保护利润
- 成交量过滤

**参数：**

- EMA: 快线(12)、慢线(26)、趋势线(55)
- RSI: 周期 14，超买 70，超卖 30
- ATR: 止损 2.5 倍，止盈 3 倍
- ADX: 阈值 20

#### 2.2 增强版策略 (`strategies/enhanced_ema_rsi_strategy.py`)

**新增功能：**

- 基于盈利交易分析的最优过滤条件
- 黑天鹅事件检测和防护
- 动态风险调整
- 更严格的入场条件

### 3. 多因子系统 (`factors/`)

#### 3.1 因子库 (`factors/factor_lib.py`)

**支持的因子类型：**

**动量因子：**

- 收益率动量 (MOM_5, MOM_10, MOM_20, MOM_60)
- 动量排名 (MOM_RANK)
- 变动率 (ROC)
- 动量加速度 (ACCELERATION)

**波动率因子：**

- 历史波动率 (VOL_10, VOL_20, VOL_60)
- 波动率比率 (VOL_RATIO)
- ATR 百分比 (ATR_PCT)
- Parkinson 波动率 (PARKINSON_VOL)

**成交量因子：**

- 成交量动量 (VOL_MOM)
- 价量趋势 (PVT)
- OBV 斜率 (OBV_SLOPE)
- VWAP 偏离 (VWAP_DEV)
- 资金流量指数 (MFI)

**趋势因子：**

- 趋势强度 (TREND_STRENGTH)
- EMA 交叉强度 (EMA_CROSS)
- ADX 因子 (ADX_FACTOR)
- 价格通道位置 (CHANNEL_POS)

**反转因子：**

- RSI 因子 (RSI_FACTOR)
- 布林带因子 (BB_FACTOR)
- 均值回归 (MEAN_REV)
- 随机指标 (STOCH_FACTOR)

**复合因子：**

- 聪明钱因子 (SMART_MONEY)
- 动量质量 (MOM_QUALITY)

#### 3.2 因子引擎 (`factors/factor_engine.py`)

**功能：**

- 因子标准化（Z-Score、排名、MinMax）
- 因子合成和加权
- 因子 IC 分析
- 信号生成

### 4. 回测引擎 (`backtester/backtest.py`)

**功能：**

- 完整的交易模拟（开仓、平仓、止损、止盈）
- 滑点和手续费模拟
- 完整的统计指标计算

**统计指标：**

- 基础统计：总收益、收益率
- 交易统计：交易次数、胜率、盈亏比
- 风险指标：最大回撤、夏普比率、索提诺比率、卡尔马比率
- 年化指标：年化收益、年化波动率

### 5. 盈利交易分析 (`analysis/`)

#### 5.1 交易分析器 (`analysis/trade_analyzer.py`)

**功能：**

- 分析盈利交易的共同特征
- 对比盈利与亏损交易的因子差异
- 提取最优因子区间
- 生成详细分析报告

#### 5.2 分析脚本 (`analysis/analyze_winning_trades.py`)

**功能：**

- 一键分析盈利交易
- 自动提取最优过滤条件
- 保存分析结果

### 6. 风险管理 (`risk/black_swan_protection.py`)

**功能：**

- 极端价格波动检测（单根 K 线>10%）
- 波动率异常检测（Z-score>3）
- 成交量异常检测（>5 倍平均）
- 连续大幅下跌检测
- 流动性危机检测
- 动态止损调整
- 仓位大小调整
- 交易暂停机制

---

## 快速开始

### 1. 环境要求

```bash
Python >= 3.8
```

### 2. 安装依赖

```bash
pip install -r requirements.txt
```

主要依赖：

- pandas
- numpy
- ccxt
- matplotlib
- pyarrow

### 3. 基本回测

```bash
# 使用默认配置运行回测
python main.py --mode backtest

# 指定交易对和时间框架
python main.py --mode backtest --symbol BTC/USDT --timeframe 30m --limit 20000

# 不显示图表
python main.py --mode backtest --no-chart
```

---

## 详细功能说明

### 功能 1: 基础回测

**用途：** 使用 EMA+RSI 策略进行回测

**命令示例：**

```bash
# 基础回测（30分钟K线，20000根）
python main.py --mode backtest --symbol BTC/USDT --timeframe 30m --limit 20000

# 快速回测（1小时K线，1000根）
python main.py --mode backtest --symbol BTC/USDT --timeframe 1h --limit 1000

# 4小时K线回测
python main.py --mode backtest --timeframe 4h --limit 2000

# 不显示图表（加快速度）
python main.py --mode backtest --symbol BTC/USDT --timeframe 1h --limit 1000 --no-chart
```

**输出：**

- 回测报告（收益、胜率、最大回撤等）
- 交易明细
- 可视化图表（保存在 `logs/` 目录）

### 功能 2: 多因子策略回测

**用途：** 使用多因子策略进行回测

**命令示例：**

```bash
# 多因子策略回测（4小时K线，2000根）
python main.py --mode factor --timeframe 4h --limit 2000

# 指定交易对的多因子回测
python main.py --mode factor --symbol BTC/USDT --timeframe 30m --limit 20000

# ETH/USDT 多因子策略
python main.py --mode factor --symbol ETH/USDT --timeframe 1h --limit 10000
```

**特点：**

- 综合多个因子生成信号
- 因子标准化和加权
- 更智能的信号过滤

### 功能 3: 因子分析

**用途：** 分析各个因子的有效性（IC、ICIR 等），查看当前因子状态和信号

**命令示例：**

```bash
# 因子分析（4小时K线，1000根）
python main.py --mode analysis --timeframe 4h --limit 1000

# 详细因子分析（30分钟K线，5000根）
python main.py --mode analysis --symbol BTC/USDT --timeframe 30m --limit 5000

# 指定交易对的因子分析
python main.py --mode analysis --symbol ETH/USDT --timeframe 1h --limit 3000
```

**输出：**

- 因子 IC 分析报告
- 因子暴露度
- 综合因子得分
- 交易信号建议

### 功能 4: 参数优化

**用途：** 自动优化策略参数（EMA、RSI 等）

**命令：**

```bash
python main.py --mode optimize --symbol BTC/USDT --timeframe 30m --limit 5000
```

**输出：**

- 最优参数组合
- 对应的夏普比率、收益率、最大回撤

### 功能 5: 盈利交易分析 ⭐

**用途：** 分析盈利交易特征，提取最优过滤条件

**命令：**

```bash
python analysis/analyze_winning_trades.py --symbol BTC/USDT --timeframe 30m --limit 20000
```

**输出：**

- 盈利交易特征分析报告
- 最优因子区间
- 分析结果 JSON 文件（保存在 `analysis/results/`）

**使用场景：**

1. 回测后发现胜率较低
2. 想要优化策略过滤条件
3. 了解盈利交易的特征

### 功能 6: 增强版策略回测

**用途：** 使用基于盈利交易分析优化的策略

**命令：**

```bash
python example_enhanced_strategy.py
```

**特点：**

- 整合最优过滤条件
- 黑天鹅事件防护
- 动态风险调整

---

## 命令行使用

### 主程序 (`main.py`)

#### 基本语法

```bash
python main.py [选项]
```

#### 参数说明

| 参数          | 说明       | 默认值       | 示例                                         |
| ------------- | ---------- | ------------ | -------------------------------------------- |
| `--mode`      | 运行模式   | `backtest`   | `backtest`, `factor`, `optimize`, `analysis` |
| `--symbol`    | 交易对     | `BTC/USDT`   | `ETH/USDT`, `BTC/USDT`                       |
| `--timeframe` | 时间框架   | `1h`         | `30m`, `1h`, `4h`, `1d`                      |
| `--limit`     | K 线数量   | `1000`       | `5000`, `20000`                              |
| `--capital`   | 初始资金   | 使用配置文件 | `10000`                                      |
| `--no-chart`  | 不显示图表 | `False`      | -                                            |
| `--no-proxy`  | 禁用代理   | `False`      | -                                            |

#### 使用示例

**基础回测（EMA+RSI 策略）：**

```bash
# 1. 默认配置回测
python main.py --mode backtest

# 2. 指定交易对和时间框架
python main.py --mode backtest --symbol BTC/USDT --timeframe 1h --limit 1000

# 3. 30分钟K线，20000根（推荐用于详细分析）
python main.py --mode backtest --symbol BTC/USDT --timeframe 30m --limit 20000

# 4. 4小时K线，2000根
python main.py --mode backtest --timeframe 4h --limit 2000

# 5. ETH/USDT 1小时K线
python main.py --mode backtest --symbol ETH/USDT --timeframe 1h --limit 5000
```

**多因子策略回测：**

```bash
# 1. 多因子策略回测（4小时K线，2000根）
python main.py --mode factor --timeframe 4h --limit 2000

# 2. 指定交易对的多因子回测
python main.py --mode factor --symbol BTC/USDT --timeframe 1h --limit 10000

# 3. ETH/USDT 多因子策略
python main.py --mode factor --symbol ETH/USDT --timeframe 30m --limit 5000
```

**因子分析：**

```bash
# 1. 因子分析 - 查看当前因子状态和信号（4小时K线，1000根）
python main.py --mode analysis --timeframe 4h --limit 1000

# 2. 指定交易对的因子分析
python main.py --mode analysis --symbol BTC/USDT --timeframe 1h --limit 5000

# 3. 详细因子分析（更多数据）
python main.py --mode analysis --symbol BTC/USDT --timeframe 30m --limit 10000
```

**参数优化：**

```bash
# 1. 优化策略参数
python main.py --mode optimize --symbol BTC/USDT --timeframe 1h --limit 5000

# 2. 4小时K线参数优化
python main.py --mode optimize --timeframe 4h --limit 3000
```

**其他选项：**

```bash
# 1. 不显示图表（加快运行速度）
python main.py --mode backtest --no-chart

# 2. 禁用代理
python main.py --mode backtest --no-proxy

# 3. 指定初始资金
python main.py --mode backtest --capital 10000 --symbol BTC/USDT --timeframe 1h --limit 1000

# 4. 组合使用：不显示图表 + 禁用代理
python main.py --mode backtest --no-chart --no-proxy --symbol BTC/USDT --timeframe 1h --limit 1000
```

**常用命令速查：**

```bash
# 快速回测（1小时K线，1000根）
python main.py --symbol BTC/USDT --timeframe 1h --limit 1000

# 因子分析（4小时K线）
python main.py --mode analysis --timeframe 4h --limit 1000

# 因子策略回测（4小时K线）
python main.py --mode factor --timeframe 4h --limit 2000

# 传统EMA策略回测（4小时K线）
python main.py --mode backtest --timeframe 4h --limit 2000
```

### 盈利交易分析 (`analysis/analyze_winning_trades.py`)

#### 基本语法

```bash
python analysis/analyze_winning_trades.py [选项]
```

#### 参数说明

| 参数          | 说明     | 默认值     |
| ------------- | -------- | ---------- |
| `--symbol`    | 交易对   | `BTC/USDT` |
| `--timeframe` | 时间框架 | `30m`      |
| `--limit`     | K 线数量 | `20000`    |

#### 使用示例

```bash
# 分析BTC/USDT 30分钟K线的盈利交易
python analysis/analyze_winning_trades.py --symbol BTC/USDT --timeframe 30m --limit 20000

# 分析ETH/USDT 1小时K线
python analysis/analyze_winning_trades.py --symbol ETH/USDT --timeframe 1h --limit 15000
```

---

## Python API 使用

### 1. 基础回测

```python
from config.settings import default_config
from data.fetch_data import DataFetcher
from strategies.ema_rsi_strategy import EmaRsiStrategy
from backtester.backtest import Backtester

# 获取数据
fetcher = DataFetcher(exchange_name='okx', cache_enabled=True)
df = fetcher.fetch_ohlcv('BTC/USDT', '30m', 20000)

# 初始化策略
strategy = EmaRsiStrategy(risk_per_trade=0.02)
df = strategy.generate_signals(df)

# 运行回测
backtester = Backtester(initial_capital=1000, fee_rate=0.001)
df, result = backtester.run(df)

# 打印报告
backtester.print_report(result)
```

### 2. 多因子策略

```python
from factors.factor_engine import FactorStrategy

# 创建多因子策略
strategy = FactorStrategy(
    lookback_period=100,
    signal_threshold=0.25,
    min_holding_period=12,
    stop_loss_atr=2.0,
    take_profit_atr=4.0
)

df = strategy.generate_signals(df)
```

### 3. 盈利交易分析

```python
from analysis.trade_analyzer import TradeAnalyzer
from backtester.backtest import Backtester

# 运行回测获取交易记录
backtester = Backtester(initial_capital=1000)
df, result = backtester.run(df)

# 分析盈利交易
analyzer = TradeAnalyzer()
analysis = analyzer.analyze_trades(result.trades, df)

# 打印分析报告
analyzer.print_analysis_report(analysis)

# 提取最优过滤条件
optimal_filters = analyzer.extract_optimal_filters(analysis)
```

### 4. 增强版策略

```python
from strategies.enhanced_ema_rsi_strategy import EnhancedEmaRsiStrategy

# 加载最优过滤条件（从分析结果中获取）
optimal_filters = {
    'RSI': {'min': 40, 'max': 65, 'mean': 52},
    'ADX': {'min': 25, 'max': 45, 'mean': 35},
    'VOLUME_RATIO': {'min': 1.0, 'max': 2.5, 'mean': 1.5},
    # ... 其他过滤条件
}

# 创建增强版策略
strategy = EnhancedEmaRsiStrategy(
    optimal_filters=optimal_filters,
    enable_black_swan_protection=True
)

df = strategy.generate_signals(df)
```

### 5. 因子分析

```python
from factors.factor_lib import FactorCalculator
from factors.factor_engine import FactorEngine

# 计算因子
calc = FactorCalculator(df)
factors = calc.calculate_all()

# 因子分析
engine = FactorEngine()
future_returns = df['close'].pct_change(10).shift(-10)
analysis = engine.factor_analysis(factors, future_returns)

# 打印报告
engine.print_factor_report(analysis)
```

---

## 配置文件说明

配置文件位于 `config/settings.py`，主要配置项：

### 数据配置

```python
data = DataConfig(
    symbol="BTC/USDT",      # 交易对
    timeframe="1h",         # 时间框架
    limit=1000,             # K线数量
    cache_enabled=True,     # 启用缓存
    cache_dir="data/cache"  # 缓存目录
)
```

### 策略配置

```python
strategy = StrategyConfig(
    risk_per_trade=0.02,           # 每笔交易风险比例（2%）
    use_volume_filter=True,        # 使用成交量过滤
    min_signal_strength=0.2       # 最小信号强度
)
```

### 回测配置

```python
backtest = BacktestConfig(
    initial_capital=1000,    # 初始资金
    fee_rate=0.001,          # 手续费率（0.1%）
    slippage=0.0005,        # 滑点（0.05%）
    use_stop_loss=True,      # 使用止损
    use_take_profit=True     # 使用止盈
)
```

### 代理配置

```python
proxy = ProxyConfig(
    enabled=True,                    # 启用代理
    http="http://127.0.0.1:7890",  # HTTP代理
    https="http://127.0.0.1:7890"   # HTTPS代理
)
```

---

## 工作流程建议

### 场景 1: 新策略开发

1. **获取数据**

   ```bash
   python main.py --mode backtest --symbol BTC/USDT --timeframe 30m --limit 20000
   ```

2. **分析结果**

   - 查看回测报告
   - 检查胜率、盈亏比、最大回撤

3. **优化策略**

   ```bash
   # 分析盈利交易
   python analysis/analyze_winning_trades.py --symbol BTC/USDT --timeframe 30m --limit 20000

   # 使用增强版策略
   python example_enhanced_strategy.py
   ```

### 场景 2: 因子研究

1. **因子分析**

   ```bash
   python main.py --mode analysis --symbol BTC/USDT --timeframe 1h --limit 5000
   ```

2. **查看因子 IC**

   - 关注 ICIR 高的因子
   - 检查因子暴露度

3. **多因子策略回测**
   ```bash
   python main.py --mode factor --symbol BTC/USDT --timeframe 1h --limit 10000
   ```

### 场景 3: 参数优化

1. **运行优化**

   ```bash
   python main.py --mode optimize --symbol BTC/USDT --timeframe 1h --limit 5000
   ```

2. **应用最优参数**
   - 修改策略参数
   - 重新回测验证

### 场景 4: 策略改进（推荐流程）

1. **初始回测**

   ```bash
   python main.py --mode backtest --symbol BTC/USDT --timeframe 30m --limit 20000
   ```

2. **分析盈利交易**

   ```bash
   python analysis/analyze_winning_trades.py --symbol BTC/USDT --timeframe 30m --limit 20000
   ```

3. **提取最优过滤条件**

   - 查看分析报告中的最优因子区间
   - 记录到配置中

4. **使用增强版策略**

   ```bash
   python example_enhanced_strategy.py
   ```

5. **对比结果**
   - 对比改进前后的回测结果
   - 验证改进效果

---

## 输出文件说明

### 回测图表

位置：`logs/backtest_YYYYMMDD_HHMMSS.png`

包含：

- 价格和信号图
- 权益曲线
- 回撤曲线
- 技术指标图

### 分析结果

位置：`analysis/results/winning_trades_analysis_YYYYMMDD_HHMMSS.json`

包含：

- 因子差异分析
- 最优过滤条件
- 回测摘要

### 数据缓存

位置：`data/cache/`

格式：Parquet 文件

- 提高数据加载速度
- 减少 API 请求

---

## 常见问题

### Q1: 数据获取失败怎么办？

**A:**

1. 检查网络连接
2. 检查代理设置（`config/settings.py`）
3. 使用 `--no-proxy` 禁用代理
4. 检查交易所 API 限制

### Q2: 如何修改策略参数？

**A:**

1. 直接修改策略类参数
2. 或在创建策略实例时传入参数：
   ```python
   strategy = EmaRsiStrategy(
       ema_fast=15,
       ema_slow=30,
       rsi_period=14
   )
   ```

### Q3: 如何添加新的因子？

**A:**

1. 在 `factors/factor_lib.py` 的 `FactorCalculator` 类中添加方法
2. 在 `calculate_all()` 中调用新因子
3. 在 `factors/factor_engine.py` 中添加权重配置

### Q4: 如何自定义回测参数？

**A:**

```python
backtester = Backtester(
    initial_capital=10000,  # 初始资金
    fee_rate=0.001,        # 手续费
    slippage=0.0005,      # 滑点
    use_stop_loss=True,    # 止损
    use_take_profit=True   # 止盈
)
```

### Q5: 盈利交易分析结果如何使用？

**A:**

1. 运行分析脚本获取最优过滤条件
2. 将过滤条件传递给增强版策略：
   ```python
   strategy = EnhancedEmaRsiStrategy(
       optimal_filters=optimal_filters  # 从分析结果中获取
   )
   ```

### Q6: 黑天鹅防护如何工作？

**A:**

- 自动检测极端市场事件
- 检测到高风险时：
  - 收紧止损
  - 减少仓位
  - 暂停开新仓
- 可在策略中启用/禁用

### Q7: 如何提高回测速度？

**A:**

1. 启用数据缓存（默认已启用）
2. 减少 K 线数量
3. 使用 `--no-chart` 不生成图表

### Q8: 回测结果不理想怎么办？

**A:**

1. 运行盈利交易分析，了解盈利交易特征
2. 使用增强版策略，应用最优过滤条件
3. 调整策略参数
4. 尝试不同的时间框架

---

## 系统架构

```
crypto_quant/
├── main.py                    # 主程序入口
├── config/                    # 配置模块
│   └── settings.py
├── data/                      # 数据模块
│   └── fetch_data.py
├── strategies/                # 策略模块
│   ├── ema_rsi_strategy.py
│   └── enhanced_ema_rsi_strategy.py
├── factors/                   # 因子模块
│   ├── factor_lib.py
│   └── factor_engine.py
├── backtester/                # 回测模块
│   └── backtest.py
├── analysis/                  # 分析模块
│   ├── trade_analyzer.py
│   └── analyze_winning_trades.py
├── risk/                      # 风险管理模块
│   └── black_swan_protection.py
├── logs/                      # 日志和图表
└── data/cache/                # 数据缓存
```

---

## 更新日志

### v3.0 (当前版本)

- ✅ 添加盈利交易分析功能
- ✅ 添加黑天鹅事件防护
- ✅ 增强版策略（基于分析结果优化）
- ✅ 多因子策略系统
- ✅ 完整的因子库

### v2.2

- EMA+RSI 策略
- 基础回测功能
- 数据获取和缓存

---

## 技术支持

如有问题，请检查：

1. 配置文件是否正确
2. 依赖是否完整安装
3. 网络连接是否正常
4. 数据是否成功获取

---

**祝交易顺利！** 📈
